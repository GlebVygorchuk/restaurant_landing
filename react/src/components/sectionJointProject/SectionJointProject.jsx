import SectionTitle from "../../assets/sectionTitle/SectionTitle"

export default function SectionJointProject() {
    return (
        <section className="joint-project">
            <SectionTitle name={'Совместный проект'} />
        </section>
    )
}